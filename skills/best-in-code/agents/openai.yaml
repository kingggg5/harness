interface:
  display_name: "Harness: Best in Code"
  short_description: "Executable delivery graphs, portable memory, evals, and QA"
  default_prompt: "Use $best-in-code to select the smallest safe workflow, compile only bounded relevant context, preserve canonical project memory, enforce capability and approval boundaries, verify the result with behavioral and deterministic evidence, and pause at material human gates."

policy:
  allow_implicit_invocation: true
